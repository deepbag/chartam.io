import React from "react";
import { Box, Container, Typography } from "@mui/material";

const Footer = () => {
  return (
    <Container sx={{ background: "#000" }} maxWidth={false}>
      <Typography>Header</Typography>
    </Container>
  );
};

export default Footer;
